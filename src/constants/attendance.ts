/** Number of consecutive absences that triggers a red flag. */
export const ATTENDANCE_CONSECUTIVE_THRESHOLD = 3

/** Total accumulated absences that triggers a red flag. */
export const ATTENDANCE_ACCUMULATED_THRESHOLD = 5
